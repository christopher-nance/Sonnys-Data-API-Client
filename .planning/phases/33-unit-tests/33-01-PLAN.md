---
phase: 33-unit-tests
plan: 01
type: execute
---

<objective>
Unit tests for labor data fetching and cost aggregation — `_fetch_all_clock_entries()` and `total_labor_cost()`.

Purpose: Verify the labor data pipeline from bulk clock entry fetching through cost aggregation, ensuring correct site filtering, date handling, and arithmetic.
Output: `tests/test_stats_labor.py` with comprehensive test coverage for the two core labor methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Key source files:
@src/sonnys_data_client/resources/_stats.py
@src/sonnys_data_client/types/_stats.py
@src/sonnys_data_client/types/_employees.py
@src/sonnys_data_client/_date_utils.py

# Established test patterns:
@tests/test_resources.py

**Established patterns:**
- `unittest.mock.MagicMock` for mocking client internals
- `_make_client()` helper creates `SonnysClient("id", "key")` with mocked rate limiter
- Test classes organized by feature (`TestListAutoPaginated`, `TestDetailGet`, etc.)
- pytest as test runner, no fixtures needed beyond mock setup

**Constraining decisions:**
- Phase 27: `_fetch_all_clock_entries()` uses `start.isoformat()[:10]` for date extraction, not `_resolve_dates()`
- Phase 27: No active employee filtering — all employees fetched, zero-entry ones contribute zero cost
- Phase 29: `total_labor_cost()` follows `total_sales()` pattern — single-pass loop, dedicated result model
- Phase 28: `LaborCostResult` has 7 fields: total_cost, regular_cost, overtime_cost, regular_hours, overtime_hours, total_hours, entry_count
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for _fetch_all_clock_entries()</name>
  <files>tests/test_stats_labor.py</files>
  <action>
Create `tests/test_stats_labor.py` with a `TestFetchAllClockEntries` class. Use the same mock pattern as `test_resources.py`: create a real `SonnysClient("id", "key")` and mock its sub-resources.

For `_fetch_all_clock_entries()`, mock `client.employees.list()` to return a list of mock employee objects (each with an `employee_id` attribute), and mock `client.employees.get_clock_entries()` to return known `ClockEntry` instances.

Test cases:
1. **Two employees, one chunk each** — 2 employees with 5-day range (within 14-day limit), each returns 2 clock entries. Verify 4 total entries returned, `employees.list()` called once, `get_clock_entries()` called twice (once per employee).
2. **Date range requiring chunking** — 1 employee with 20-day range (splits into 14+6). Verify `get_clock_entries()` called twice with correct date ranges from `build_date_chunks()`.
3. **Site code filtering** — Set `client.site_code = "JOLIET"`, return entries with mixed site_codes ("JOLIET" and "OTHER"). Verify only "JOLIET" entries returned.
4. **No site code returns all entries** — `client.site_code = None`, return entries with different site_codes. Verify all entries returned unfiltered.
5. **Empty employee list** — `employees.list()` returns `[]`. Verify empty list returned, `get_clock_entries()` never called.
6. **Datetime input normalization** — Pass `datetime` objects instead of strings. Verify `.isoformat()[:10]` extraction works (the method handles both string and datetime inputs).

Use `ClockEntry` from `sonnys_data_client.types._employees` to create realistic test data. Create a helper `_make_clock_entry()` that builds a ClockEntry with sensible defaults (regular_rate=15.0, regular_hours=8.0, overtime_rate=22.5, overtime_hours=0.0, site_code="MAIN", etc.) and accepts overrides.

Mock `client._rate_limiter.acquire = MagicMock(return_value=0.0)` to prevent rate limiting in tests (following test_resources.py pattern).
  </action>
  <verify>pytest tests/test_stats_labor.py::TestFetchAllClockEntries -v passes all 6 tests</verify>
  <done>6 test methods covering bulk fetching, chunking, site filtering, empty list, and datetime normalization — all passing</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for total_labor_cost()</name>
  <files>tests/test_stats_labor.py</files>
  <action>
Add a `TestTotalLaborCost` class to `tests/test_stats_labor.py`. For these tests, mock `StatsResource._fetch_all_clock_entries()` directly (using `unittest.mock.patch.object`) to return known ClockEntry lists, isolating the aggregation logic from the fetching logic.

Test cases:
1. **Normal mix of regular and overtime** — 3 entries: one with 8h regular @ $15/hr + 0h OT, one with 8h regular @ $12/hr + 2h OT @ $18/hr, one with 6h regular @ $20/hr + 0h OT. Verify total_cost = (120 + 96 + 36 + 120) = 372.0, regular_cost = 336.0, overtime_cost = 36.0, regular_hours = 22.0, overtime_hours = 2.0, total_hours = 24.0, entry_count = 3.
2. **Empty entries (no clock data)** — `_fetch_all_clock_entries` returns `[]`. Verify all fields are 0.0/0.
3. **Only regular hours** — 2 entries with overtime_hours=0.0, overtime_rate=0.0. Verify overtime_cost=0.0, overtime_hours=0.0, total_cost equals regular_cost.
4. **Only overtime hours** — 1 entry with regular_hours=0.0, regular_rate=0.0, overtime_hours=4.0, overtime_rate=25.0. Verify regular_cost=0.0, overtime_cost=100.0, total_cost=100.0.
5. **Single entry precision** — 1 entry with regular_rate=15.75, regular_hours=7.5. Verify regular_cost = 118.125 (exact float multiplication, no rounding).

Each test creates a `StatsResource` instance with a mocked client, patches `_fetch_all_clock_entries`, calls `total_labor_cost("2026-01-01", "2026-01-31")`, and asserts the returned `LaborCostResult` fields.
  </action>
  <verify>pytest tests/test_stats_labor.py::TestTotalLaborCost -v passes all 5 tests</verify>
  <done>5 test methods covering normal aggregation, empty data, regular-only, overtime-only, and precision — all passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_stats_labor.py -v` passes all 11 tests
- [ ] `pytest` (full suite) passes with zero regressions
- [ ] No import errors or warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 11 new tests added in tests/test_stats_labor.py
- Existing 137 tests unaffected
- Test file follows established patterns from test_resources.py
</success_criteria>

<output>
After completion, create `.planning/phases/33-unit-tests/33-01-SUMMARY.md`
</output>
