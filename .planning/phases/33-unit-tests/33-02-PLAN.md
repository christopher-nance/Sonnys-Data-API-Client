---
phase: 33-unit-tests
plan: 02
type: execute
---

<objective>
Unit tests for CPC computation and report labor integration — `cost_per_car()` and the labor fields in `report()`.

Purpose: Verify the composed CPC method handles zero-division safely and the unified report correctly populates labor and cost_per_car fields.
Output: Additional test classes in `tests/test_stats_labor.py` covering CPC and report integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-unit-tests/33-01-SUMMARY.md

# Key source files:
@src/sonnys_data_client/resources/_stats.py
@src/sonnys_data_client/types/_stats.py

# Test file from Plan 01:
@tests/test_stats_labor.py

**Established patterns:**
- Phase 30: `cost_per_car()` follows `conversion_rate()` composition pattern — calls `total_labor_cost()` + `total_washes()`, divides
- Phase 31: `report()` inlines labor cost aggregation (doesn't call `total_labor_cost()`) and computes CPC from shared data
- Zero-division safety: returns 0.0 when total_washes == 0

**Constraining decisions:**
- Phase 30: CPC = total_labor_cost / total_washes (0.0 if zero washes)
- Phase 31: report() fetches clock entries alongside transactions, aggregates inline
- Phase 28: CostPerCarResult has 3 fields: cost_per_car, total_labor_cost, total_washes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for cost_per_car()</name>
  <files>tests/test_stats_labor.py</files>
  <action>
Add a `TestCostPerCar` class to `tests/test_stats_labor.py`. Mock `StatsResource.total_labor_cost()` and `StatsResource.total_washes()` via `unittest.mock.patch.object` to return known result objects, isolating the division logic.

Test cases:
1. **Normal calculation** — labor total_cost=1000.0, washes total=200. Verify cost_per_car=5.0, total_labor_cost=1000.0, total_washes=200.
2. **Zero washes (division safety)** — labor total_cost=500.0, washes total=0. Verify cost_per_car=0.0, total_labor_cost=500.0, total_washes=0.
3. **Zero labor cost** — labor total_cost=0.0, washes total=150. Verify cost_per_car=0.0, total_labor_cost=0.0, total_washes=150.
4. **Fractional result** — labor total_cost=850.0, washes total=200. Verify cost_per_car=4.25, total_labor_cost=850.0, total_washes=200.

Each test creates mock return values using `LaborCostResult(...)` and `WashResult(...)` with appropriate fields, patches both methods, calls `cost_per_car("2026-01-01", "2026-01-31")`, and asserts the returned `CostPerCarResult`.
  </action>
  <verify>pytest tests/test_stats_labor.py::TestCostPerCar -v passes all 4 tests</verify>
  <done>4 test methods covering normal calc, zero-division safety, zero labor, and fractional result — all passing</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for report() labor integration</name>
  <files>tests/test_stats_labor.py</files>
  <action>
Add a `TestReportLaborIntegration` class to `tests/test_stats_labor.py`. For report() tests, mock the underlying fetch methods that report() calls directly: `_fetch_transactions_v2()`, `_fetch_transactions_by_type()`, `_genuine_plan_sale_ids()`, and `_fetch_all_clock_entries()`.

Test cases:
1. **Report includes labor fields** — Provide 2 clock entries (one with regular hours, one with OT) and 3 v2 transactions (1 wash, 1 plan sale, 1 redemption). Verify `report.labor` is a `LaborCostResult` with correct aggregation, and `report.cost_per_car` is a `CostPerCarResult` with correct division.
2. **Report with zero washes** — Provide 1 clock entry but zero wash transactions (only recurring recharges with negative totals or empty transaction list). Verify `report.cost_per_car.cost_per_car == 0.0` (zero-division safety) and `report.labor.total_cost > 0` (labor exists even without washes).
3. **Report with no clock entries** — Provide normal transactions but `_fetch_all_clock_entries` returns `[]`. Verify `report.labor.total_cost == 0.0`, `report.labor.entry_count == 0`, and `report.cost_per_car.cost_per_car == 0.0`.

For mock setup: create `TransactionV2ListItem` instances with realistic fields (trans_id, total, is_recurring_plan_sale, is_recurring_plan_redemption, transaction_status). Mock `_fetch_transactions_by_type` to return `TransactionListItem` instances for "wash" and "recurring" type calls. Use `side_effect` to return different results based on the `item_type` argument.

Verify the report object's `labor` and `cost_per_car` attributes specifically — don't re-test sales/washes/conversion logic (those are tested elsewhere).
  </action>
  <verify>pytest tests/test_stats_labor.py::TestReportLaborIntegration -v passes all 3 tests</verify>
  <done>3 test methods covering report labor integration, zero-washes edge case, and no-clock-entries edge case — all passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_stats_labor.py -v` passes all tests (11 from plan 01 + 7 from plan 02 = 18 total)
- [ ] `pytest` (full suite) passes with zero regressions
- [ ] No import errors or warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 7 new tests added (4 CPC + 3 report integration)
- Total test_stats_labor.py: 18 tests
- Full suite: 137 + 18 = 155 tests passing
- Phase 33 complete
</success_criteria>

<output>
After completion, create `.planning/phases/33-unit-tests/33-02-SUMMARY.md`
</output>
